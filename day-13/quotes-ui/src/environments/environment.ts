// Development environment - swapped out for environment.production.ts in
// production builds via the `fileReplacements` block in angular.json.
export const environment = {
  production: false,
  apiOrigin: 'http://localhost:5296',
};
